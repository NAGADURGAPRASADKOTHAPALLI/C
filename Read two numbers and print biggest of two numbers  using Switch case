// Read two numbers and print biggest of two numbers  using Switch case
#include<stdio.h>
void main()
{
	int x,y;
	printf("enter two values:");
	scanf("%d %d",&x,&y);
	switch(x>y)
	{
		case 0:
			printf("%d",y);
			break;
			case 1:
				printf("%d",x);
				break;
	}
}
