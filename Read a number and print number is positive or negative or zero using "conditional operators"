//Read a number and print number is positive or negative or zero using "conditional operators"
#include<stdio.h>
void main()
{
	int n;
	printf("enter the number:");
	scanf("%d",&n);
	(n>0)?printf("positive"):(n==0)?printf("zero"):printf("negative");
}
